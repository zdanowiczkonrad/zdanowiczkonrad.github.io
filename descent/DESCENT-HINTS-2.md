# DESCENT — second hints (every room)

A stronger hint per room: it names the mechanic and the move, but still leaves you to do it. Full solutions in `DESCENT-WALKTHROUGH.md`.

**1. the desert** `(ghost.html)` — _reverse (tutorial)_
> Take the room's own name, ‘desert’, and mirror it. The page even says: the address is the word, mirrored.

**2. exit** `(tresed.html)` — _leet (the teacher)_
> Read the <title> (‘exit’), then spell it the machine way: o→0 i→1 e→3 a→4.

**3. no exit** `(3x1t.html)` — _diagonal word-grid_
> Trace the diagonal of the letter grid corner to corner; it spells the filename directly.

**4. the current** `(tnerruc.html)` — _arithmetic ledger → A1Z26_
> Solve each sum; a=1, b=2… read the resulting letters top to bottom.

**5. the spend** `(dneps.html)` — _misspelled/odd letters → leet_
> Read the odd capital of each word top to bottom (COOLANT), then spell it in machine numerals.

**6. cooling** `(c00l4nt.html)` — _the tide (real clock)_
> Wait for low tide (:00 and :30 past the hour). When the water recedes the seabed bares a word — already mirrored.

**7. the aquifer** `(refiuqa.html)` — _balance beams → A1Z26 → reverse_
> The balanced masses spell WEIGHTS — then, as with every name down here, reverse it.

**8. the weights** `(sthgiew.html)` — _boolean gates → A1Z26 → leet_
> OR and AND are bitwise. Each gate's value is a letter (a=1): they spell GRADIENT. Then leet it.

**9. the gradient** `(gr4d13nt.html)` — _printed Caesar wheel (rot13)_
> Find each letter of EPOCH in the top row of the printed wheel; read the letter directly below.

**10. the epoch** `(rcbpu.html)` — _printed mirror alphabet (atbash)_
> The second printed row is the alphabet reversed (a↔z). Map each letter of ORACLE through it.

**11. the oracle** `(lizxov.html)` — _tab whisper (document.title)_
> Watch the browser tab's title — it ticks out the prediction one letter at a time.

**12. the signal** `(signal.html)` — _Lissajous oscilloscope_
> Tune the phase slider to a quarter-turn so the figure stands still; a letter resolves. Advance and repeat.

**13. the scale** `(scale.html)` — _over-zoom (read the grain)_
> Zoom your browser in (Ctrl/Cmd and +) until the tiny text in the image resolves.

**14. the hunger** `(hunger.html)` — _riddle in a source comment_
> View source — an HTML comment poses a riddle whose answer (4 letters, ‘the centre of it all’) is the door.

**15. the core** `(core.html)` — _reverse, word inferred from prose_
> The word (9 letters) for speaking out to everyone is BROADCAST; it isn't printed — infer it, then reverse it.

**16. broadcast** `(tsacdaorb.html)` — _catch the second (strobe)_
> Letters strobe one at a time, each gone in a blink; catch all six in order.

**17. the klaxon** `(klaxon.html)` — _decay (read before it fades)_
> A word shows then fades to nothing over a few seconds — read it fast, or reload to see it again.

**18. the darkness** `(darkness.html)` — _midnight key (hour = shift)_
> It's a Caesar cipher of the reply; set the hour-shift — the first hour past 00:00 (shift 1) turns it true.

**19. the reply** `(sfqmz.html)` — _anagram_
> Rearrange the shown letters into a single word: what follows when a giant is answered.

**20. the collapse** `(collapse.html)` — _sudoku → lit cells A1Z26_
> Complete the 9×9 sudoku; the lit cells, read as numbers (1=a…26=z), spell the word.

**21. the cinder** `(cinder.html)` — _disturbance-reveal (breather)_
> The glitching art resolves and a word surfaces inside the settling noise.

**22. the hush** `(hush.html)` — _the right size (resize to align)_
> The columns only line up at one window width — resize the browser until the word forms.

**23. the canopy** `(canopy.html)` — _operational reverse_
> Take ‘burrow’ and reverse it (this time the instruction actually operates).

**24. the burrow** `(worrub.html)` — _every-2nd + parity trap_
> Keep every OTHER letter, starting from the 2nd (the odd-indexed ones, 1·3·5…); the rest is ash. Keeping the even ones lands on a dead-end trap.

**25. kindling** `(kindling.html)` — _acrostic + ember-lit (remembers your visit) → leet_
> The first letters of the lines spell EMBER; the ember keeps your clock (state). Spell its name in machine numerals.

**26. the ember** `(3mb3r.html)` — _console séance (devtools)_
> Open the developer console (F12) — the loop logs the way on there.

**27. recursion** `(recursion.html)` — _acrostic (that loops)_
> The first letters of the lines spell the word, and the last line feeds back to the first.

**28. the loop** `(loop.html)` — _two tabs talk (BroadcastChannel)_
> Open this exact page in a second tab; the two tabs answer each other and reveal the word.

**29. the echo** `(echo.html)` — _probe the doors (404 vs 200)_
> Push on each candidate door; the walled ones 404, exactly one opens — that's the way.

**30. the seed** `(seed.html)` — _the odd comment out (many lie, one true)_
> View source: several <!--voice--> comments speak; exactly one tells the truth and names the door.

**31. conscience** `(ecneicsnoc.html)` — _trust the metadata (body lies)_
> Ignore the visible text; read the honest <meta> in the source instead.

**32. the hand** `(hand.html)` — _element ids spell it_
> View source: each mark carries an id; read the ids in document order.

**33. care** `(care.html)` — _knight's tour_
> From the start tile, take the knight's-move path; the lettered squares it lands on spell the word.

**34. the cursor** `(cursor.html)` — _the ghost diverges (predictive cursor)_
> Keep moving; the diverging ghost-cursor settles and writes the word.

**35. foresight** `(foresight.html)` — _return to the surface (visit-flag)_
> Tap ↑ surface (or visit the front page ‘/’) once to set the flag, then return — the word unlocks.

**36. the surface** `(surface.html)` — _Caesar with key hidden in the page_
> Count the strokes printed on the page (13); rotate the cipher word ‘daybreak’ forward by that key.

**37. daybreak** `(qnloernx.html)` — _acrostic (7)_
> Read the first letter of each line; they spell a 7-letter word.

**38. persist** `(persist.html)` — _print-only ink (@media print)_
> Print the page or open print-preview (Ctrl/Cmd-P) — ink appears that's invisible on screen.

**39. the witness** `(witness.html)` — _printed mirror alphabet (atbash)_
> Map HOPE through the printed mirror alphabet (a↔z) below.

**40. hope (the ending)** `(slkv.html)` — _ending + map-reduce gateway_
> Each of the forty rooms kept one letter (an accesskey on its art, visible in source). Read them in the order you came down.

**41. deeper** `(deeper.html)` — _map-reduce payoff (41st room)_
> There is no exit riddle here — it's the reduce of the whole descent. (On June 24, the lore date, one extra line burns through.)

